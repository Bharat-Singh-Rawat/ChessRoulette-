// Token economy constants — central so they're easy to tune later.
export const TOKENS_PER_CASUAL_MATCH = 1;
export const TOKENS_FOR_BULLET_WIN = 10;
export const STARTING_TOKENS = 50;
export const BULLET_TIME_MS = 60_000; // 60s per side, no increment
